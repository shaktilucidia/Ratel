apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: ratel-backend-users
  namespace: ratel-backend

spec:
  parentRefs:
    - name: ratel-gateway

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/creatures

      backendRefs:
        - name: ratel-backend-users
          port: 9000

    - matches:
        - path:
            type: Exact
            value: /dev/swagger/creatures
        - path:
            type: Exact
            value: /dev/swagger/creatures/

      filters:
        - type: RequestRedirect
          requestRedirect:
            path:
              type: ReplaceFullPath
              replaceFullPath: /dev/swagger/creatures/index.html
            statusCode: 302

    - matches:
        - path:
            type: PathPrefix
            value: /dev/swagger/creatures

      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /swagger

      backendRefs:
        - name: ratel-backend-users
          port: 9000
