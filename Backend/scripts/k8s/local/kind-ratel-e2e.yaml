apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: ratel-e2e

nodes:
- role: control-plane
  extraPortMappings:

      - containerPort: 30002
        hostPort: 10001
        protocol: TCP

- role: worker
