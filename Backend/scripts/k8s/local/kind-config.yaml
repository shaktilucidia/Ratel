apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: ratel-testing

nodes:
- role: control-plane
  extraPortMappings:
      - containerPort: 30000
        hostPort: 9000
        protocol: TCP

      - containerPort: 30001
        hostPort: 5432
        protocol: TCP

      - containerPort: 30002
        hostPort: 9001
        protocol: TCP

- role: worker
