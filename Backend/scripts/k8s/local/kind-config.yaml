apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: ratel-testing

nodes:
- role: control-plane
  extraPortMappings:
      - containerPort: 30000
        hostPort: 9000
        protocol: TCP

- role: worker
- role: worker
