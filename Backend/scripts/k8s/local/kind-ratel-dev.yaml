apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: ratel-dev

networking:
  ipFamily: dual

nodes:
- role: control-plane
  extraPortMappings:
      - containerPort: 30001
        hostPort: 5432
        protocol: TCP

- role: worker
