apiVersion: v1
kind: Service
metadata:
  name: ratel-backend-users
  namespace: ratel-backend

spec:
  type: NodePort

  selector:
    app: ratel-backend-users

  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30000
